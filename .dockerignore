# Dependencies
node_modules/
npm-debug.log
# package-lock.json is needed for npm ci

# Git
.git/
.gitignore

# Development files
.claude/
.DS_Store

# Documentation
README.md
CLAUDE.md

# Database (will be mounted as volume)
database/*.db
database/*.db-shm
database/*.db-wal

# Credentials (will be provided via environment or secrets)
credentials.json
